{
  "name": "poopyhead",
  "version": "0.1.0",
  "private": true,
  "description": "Poopyhead card game monorepo",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend",
    "build": "npm run build --workspace backend",
    "start": "npm run start --workspace backend",
    "test": "npm run test --workspace backend",
    "test:ui": "npm run test:ui --workspace backend",
    "lint": "npm run lint --workspace backend",
    "type-check": "npm run type-check --workspace backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}